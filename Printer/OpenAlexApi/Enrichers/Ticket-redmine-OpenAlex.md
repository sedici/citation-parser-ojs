Necesito hacer un ticket para redmine, indicando la solución vieja que estaba implementada y la que ahora se va a implementar (incluyendo los patrones de diseño que elegimos), indicando además por qué se eligieron dichos patrones y qué se investigó para llegar a ese diseño (se puede explicar que hay distintas fuentes de OpenAlex, como journal, book, book-chapter, etc. que corresponden a tipos de referencia específicos también y que son compatibles con XML JATS, o casos como e-book digital, que pueden ser mapeados a book tranquilamente ya que es lo mismo, y ahí se puede explicar el mapeo que se haría en el Factory desarrollado). En cuanto a las estrategias concretas del strategy, se plantea definir una estrategia por cada tipo de referncia en texture, siempre y cuando OpenAlex tenga soporte como tipo de fuente posible, es decir, si en texture por ejemplo se pueden referenciar Magazines, pero OpenAlex no tiene una fuente especificada para esto, entonces no se podría implementar). También explicar que existe el GenericOpenAlexEnricher como fallback automático y el BaseOpenAlexEnricher que es toda la lógica en común para los tipos de fuente (como enriquecimiento de nombre de autores, título, entre otras cosas que son comunes, para no repetir código, evitando el bad smell duplicate code). También me dejaría dejar planteado esto como un desarrollo por etapas. Es decir, en una primera etapa se podrían estudiar las distintas fuentes, analizar y desarrollar los casos de prueba para cada una de las fuentes, verificando qué datos se recuperaran del JSON que devuelve la API de openAlex, para saber en cada tipo de fuente qué datos tomar y posteriormente generar su etiqueta en XML JATS para la referencia bibliográfica aprovechando la implementación de los Printers (que generan el XML JATS, ahora se declaran de nuevo los métodos en BaseOpenAlexEnricher, pero podrían aprovecharse los métodos ya creados en los printers para no repetir lógica?). En esta etapa también se tendría que analizar qué fuentes de OpenAlex son similares a otras de base, como "ebook digital", que es lo mismo que "book" y puede mapearse a book directamente. 
A su vez, en una segunda etapa se podría evaluar la utilización de otras técnicas de procesamiento de lenguaje natural (NLP o PLN) para encontrar la opción mas compatible cuando no hay DOI. A lo mejor existen algunas alternativa a la técnica de expresiones regulares que actualmente se utilizan para extraer la información de cada referencia en texto plano. A su vez, se podría evaluar la utilización de la busqueda por título que ofrece openAlex, donde se puede enviar a la API el título de un artículo, y OpenAlex retorna los resultados posibles y las ocurrencias de ese título. En este caos, existe la posibilidad de que los resultados que retorna OpenAlex para un título sean mayores a uno (es decir que un mismo título retorna mas de un resultado posible). En este caso se puede procesar cada uno de los resultados y compararlo con datos de la propia referencia (por ejemplo el procesamiento de autores que ya se hace, para ver si los autores devueltos por OpenAlex son los mismos que los que se extrajeron mediante regex en la referencia en texto plano), o también utilizar otros datos, como la fecha de la referencia por ejemplo, para ir descartando opciones y quedarse  con el recurso en json que corresponda con la referencia que se quiera enriquecer. Esto serviría para enriquecer utilizando al api de open alex aunque en la referencia en texto plano no se haya indicado un DOI. Se podría evaluar haciendo pruebas, donde se use el script implementado (que dice si el doi existe o no en la base de datos de OpenAlex) y para esas referencias para las cuales el doi no existe en OpenAlex, agarrar el título y enviarselo a la API de OpenAlex, para ver si efectivamente existe de igual manera ese artículo en OpenAlex, mas allá de que su DOI figure como NO registrado en OpenAlex.
En una tercera etapa se podría implementar mejor la integración de enriquecimiento de instituciones autoras en OpenAlex. Actualmente, si se identifica una institución en la referencia bibliográfica, es enviada a OpenAlex para validar su existencia en la misma, y si no existe no recuerdo si el código arrojaba error o no (si arroja error cuando no existe, se debería ajustar el código ya que puede ser que haya instituciones que no estén registradas en OpenAlex pero sí existan realmente, por ende estaría mal tirar un error por eso). Por ahora, la API para insti